package java.controller;

import java.model.Note;
import java.view.NoteView;

public class NoteController {
    private Note model;
    private NoteView view;

    public NoteController(Note model, NoteView view) {
        this.model = model;
        this.view = view;
    }

    // create getter and setter methods for model variables -> control model object

    // control view object
}
